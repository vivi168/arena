<%= semantic_form_for @fight do |f| %>
  <%= f.input :home %>
  <%= f.input :away %>
  <%= f.actions do %>
    <%= f.action :submit, as: :button, button_html: { class: 'btn btn-primary' } %>
  <% end %>
<% end %>
